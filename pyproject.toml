[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-debate"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "edge-tts>=6.1",
]


[tool.setuptools.packages.find]
include = ["debate_framework*", "live*"]

